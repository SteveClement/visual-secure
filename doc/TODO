== from LisaStopLine discussion ==

- traceroute-circl output in the logs
- whois output of all associated domains/IP address
- make mirroring optional
- site interface to include the data

== from CIRCL ==

- Add a screenshot of the website using a headless Firefox to complement
  the wget mirror.
- https://github.com/bard/mozrepl/wiki add MozRepl interface to Firefox
  instance and interface it to run a "Save As...". Checked the API. It works
  for a controlling firefox from a TCP interface but can't find a way to call
  some inner Firefox functions.
- Improve GUI especially reporting

